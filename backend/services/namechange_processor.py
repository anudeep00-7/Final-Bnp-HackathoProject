class NameChangeProcessor:

    def process(
        self,
        current_name,
        new_name
    ):

        return {
            "old_name": current_name,
            "new_name": new_name
        }